exports.handler = async function (event, context) {
//mensagens.length
    //var i = Math.floor(Math.random() * mensagens.length)


    return {
      statusCode: 200,
      body: "Essa é uma mensagem de teste, testando pra ver se vai funcionar o baguho!",
    };
  };